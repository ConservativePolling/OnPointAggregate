# =============================================================================
# Congress.gov API Key (REQUIRED for live member/bill data)
# =============================================================================
# Without this key, the site will display placeholder/demo data.
# Get your FREE API key at: https://api.congress.gov/sign-up/
#
# For Vercel deployment: Add this as an environment variable in your
# Vercel project settings: Settings → Environment Variables
# =============================================================================
CONGRESS_API_KEY=your_api_key_here

# =============================================================================
# Optional Configuration
# =============================================================================

# Limit bill pages fetched (default: 20 pages = 5,000 bills)
# Set to 0 for unlimited
CONGRESS_BILL_PAGE_LIMIT=20

# Filter by Congress session (e.g., "119" for 119th Congress)
CONGRESS_SESSION=119

# Override session number (1 or 2, default: computed from year)
# CONGRESS_SESSION_NUMBER=1

# Override roll call year (default: computed from Congress session)
# ROLLCALL_YEAR=2025

# Bill fetching timeout in milliseconds (default: 60000)
# CONGRESS_BILL_TIMEOUT_MS=60000
